export async function getItem(env, slug){const r=await fetch(`${env.SUPABASE_URL}/rest/v1/archive_items?select=*&slug=eq.${encodeURIComponent(slug)}&status=eq.active&limit=1`,{headers:{apikey:env.SUPABASE_KEY,Authorization:`Bearer ${env.SUPABASE_KEY}`}});if(!r.ok)return null;const a=await r.json();return a[0]||null}
export async function proxySource(item,request,disposition='inline'){const source=item.source_url;const range=request.headers.get('range');const h={};if(range)h.Range=range;const r=await fetch(source,{headers:h,redirect:'follow'});const out=new Headers();for(const k of ['content-type','content-length','content-range','accept-ranges','etag','last-modified','cache-control']){const v=r.headers.get(k);if(v)out.set(k,v)}out.set('content-disposition',`${disposition}; filename="${(item.title||'archive').replace(/[\r\n"\\]/g,'_')}"`);out.set('x-content-source','AWEARCHIVE');return new Response(r.body,{status:r.status,headers:out})}