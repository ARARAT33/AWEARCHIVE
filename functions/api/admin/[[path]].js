import {isAdmin,makeSession,cookie,clearCookie} from '../../_shared/auth.js';
import {readRepoFile,writeRepoFile} from '../../_shared/github.js';
const out=(d,s=200,h={})=>Response.json(d,{status:s,headers:h});
export async function onRequest(context){const req=context.request,env=context.env,path=context.params.path||'';if(req.method==='POST'&&path==='login'){const b=await req.json().catch(()=>({}));if(b.username!==env.ADMIN_USERNAME||b.password!==env.ADMIN_PASSWORD)return out({error:'Invalid credentials'},401);const session=await makeSession(env,b.username);return out({ok:true},{200:{},headers:{'Set-Cookie':cookie(session)}})}if(req.method==='POST'&&path==='logout')return out({ok:true},200,{'Set-Cookie':clearCookie()});if(!await isAdmin(req,env))return out({error:'Unauthorized'},401);if(req.method==='GET')return out({ok:true,admin:true});const f=await readRepoFile(env,'data/archive.json');let items=Array.isArray(f.data)?f.data:[];if(req.method==='PUT'){const x=await req.json();if(!x.title||!x.url||!x.type)return out({error:'title, url and type are required'},400);x.id=x.id||crypto.randomUUID();x.slug=x.slug||x.id.toLowerCase().replace(/[^a-z0-9-]+/g,'-').replace(/^-|-$/g,'');x.tags=Array.isArray(x.tags)?x.tags:[];x.status=x.status||'active';const now=new Date().toISOString(),i=items.findIndex(a=>a.id===x.id);if(i>=0){x.createdAt=items[i].createdAt||now;items[i]={...items[i],...x,updatedAt:now};}else{items.unshift({...x,createdAt:now,updatedAt:now});}await writeRepoFile(env,'data/archive.json',items,f.sha,`admin: ${i>=0?'update':'add'} archive item ${x.slug}`);return out({ok:true,item:x});}if(req.method==='DELETE'){const b=await req.json().catch(()=>({}));if(!b.id)return out({error:'id required'},400);items=items.filter(x=>x.id!==b.id);await writeRepoFile(env,'data/archive.json',items,f.sha,`admin: delete archive item ${b.id}`);return out({ok:true});}return out({error:'Method not allowed'},405)}
