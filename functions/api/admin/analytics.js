import {isAdmin} from '../../_shared/auth.js';
import {readRepoFile} from '../../_shared/github.js';
const day=d=>d.toISOString().slice(0,10);
export async function onRequest(context){if(!await isAdmin(context.request,context.env))return Response.json({error:'Unauthorized'},{status:401});const days=Number(new URL(context.request.url).searchParams.get('days')||7);const now=Date.now(),all=[];for(let i=0;i<Math.min(Math.max(days,1),30);i++){const d=new Date(now-i*86400000);const f=await readRepoFile(context.env,`data/analytics/${day(d)}.json`);if(Array.isArray(f.data))all.push(...f.data)}const unique=new Map(),countries={},items={},paths={},ips={},refs={};for(const e of all){if(e.visitor)unique.set(e.visitor,e);if(e.country)countries[e.country]=(countries[e.country]||0)+1;if(e.item)items[e.item]=(items[e.item]||0)+1;if(e.path)paths[e.path]=(paths[e.path]||0)+1;if(e.ip)ips[e.ip]=(ips[e.ip]||0)+1;if(e.referrer){let r;try{r=new URL(e.referrer).hostname}catch{r=e.referrer}refs[r]=(refs[r]||0)+1}}const recent=all.filter(e=>now-new Date(e.ts).getTime()<300000).sort((a,b)=>b.ts.localeCompare(a.ts)).slice(0,100);const top=o=>Object.entries(o).sort((a,b)=>b[1]-a[1]).slice(0,50).map(([key,count])=>({key,count}));return Response.json({generatedAt:new Date().toISOString(),events:all.length,visitors:unique.size,countries:top(countries),items:top(items),paths:top(paths),ips:top(ips),referrers:top(refs),live:recent})}
